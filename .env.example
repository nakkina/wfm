# Copy to .env and adjust. No secrets are needed for this local POC.

# Config file (relative to wfm/). Copy config/wfm.example.yaml to config/wfm.yaml for local changes.
WFM_CONFIG_PATH=config/wfm.example.yaml
